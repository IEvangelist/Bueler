[package]
name = "oxide-core"
description = "Reactive runtime and signal system for the Oxide framework"
version.workspace = true
edition.workspace = true
license.workspace = true

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3"
